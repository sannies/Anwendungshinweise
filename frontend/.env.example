# Lokale Entwicklung: Basis-URL der deployten REST-API.
# Nach `cdk deploy` erscheint der Wert als Stack-Output "ApiUrl".
# Beispiel:
VITE_API_BASE_URL=https://abc123.execute-api.eu-central-1.amazonaws.com/prod/
